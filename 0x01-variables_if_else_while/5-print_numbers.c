#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - entry point
 *
 * Return: zero
 */
int main(void)
{
	int chr;

	for (chr = 0; chr < 10; chr++)
	{
		putchar(chr);
	}
	putchar('\n');
	return (0);
}
