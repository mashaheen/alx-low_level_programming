#include"main.h"
/**
 * _isdigit - print alphabet
 * @c: argument description
 *
 * Return: 1 if is alphaet, and z otherwise
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	return (0);
}
