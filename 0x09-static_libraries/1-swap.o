#include"main.h"
/**
 * swap_int - print alphabet
 * @a: argument description
 * @b: argument desc
 *
 * Return: void
 */
void swap_int(int *a, int *b)
{
	int temp;

	temp = *a;
	*a = *b;
	*b = temp;
}
