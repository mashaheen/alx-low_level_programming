#include"main.h"
/**
 * factorial - print alphabet
 * @n: argument description
 *
 * Return: pointer to newchar
 */
int factorial(int n)
{
	if (n < 0)
	{
		return (-1);
	}
	if (n == 0)
	{
		return (1);
	}
	return ((factorial(n - 1) * n));
}
